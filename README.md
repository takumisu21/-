### 👤 ユースケース図

```mermaid
usecaseDiagram
    actor User as "ユーザー (利用者)"
    
    rectangle "サブスク管理 Hub & Sticker Arcade" {
        usecase UC1 as "新規サブスクリプションを登録する"
        usecase UC2 as "登録中のサブスクを解約する\n(節約金からメダルを獲得)"
        usecase UC3 as "サブスクを一覧から完全削除する"
        usecase UC4 as "現在の月額合計コストを確認する"
        usecase UC5 as "カレンダーで更新スケジュールを確認する"
        usecase UC6 as "メダルを消費してアニマルガチャを回す"
        usecase UC7 as "シールリストからシールを選んで画面に貼る"
        usecase UC8 as "画面上のシールをダブルクリックで回収する"
    }
    
    User --> UC1
    User --> UC2
    User --> UC3
    User --> UC4
    User --> UC5
    User --> UC6
    User --> UC7
    User --> UC8

    ### 2. クラス図 (Class Diagram)
JavaScript内部のデータ構造（`SubscriptionManager`クラスや、`localStorage`に保存される`appState`オブジェクトなど）の定義と関連性を明示した図です。

```markdown
### 🏗️ クラス図

```mermaid
classDiagram
    class SubscriptionManager {
        +array subscriptions
        +constructor()
        +save()
        +add(name, price, cycle, startDate)
        +cancel(id)
        +delete(id)
        +calculateTotalMonthlyCost() int
    }

    class Subscription {
        +string id
        +string name
        +int price
        +string cycle
        +string startDate
        +string endDate
    }

    class appState {
        +int ownedMedals
        +object inventory
        +array placedStickers
    }

    class PlacedSticker {
        +string id
        +string emoji
        +int x
        +int y
    }

    class gachaPoolItem {
        +string id
        +string emoji
        +string name
        +string type
    }

    SubscriptionManager "1" --> "*" Subscription : 登録データを管理
    appState "1" --> "*" PlacedSticker : 配置されたシールの座標を保持
    appState "1" ..> gachaPoolItem : ガチャ結果をinventory(ID毎の枚数)に蓄積

    ### 3. 協調図 / シーケンス図 (Sequence Diagram)
※Mermaidはオブジェクトが網の目のように繋がる厳密な「協調図」の記法を直接サポートしていないため、実習課題で役割を完全に代替できる**シーケンス図（時系列のオブジェクト間相互作用）**で美しく表現しています。

ユーザーがサブスクを解約してメダルをゲットし、そのメダルでガチャを回してシールを貼るまでのデータ構造のやり取りがひと目で分かります。

```markdown
### 🤝 相互作用図 (シーケンス)

```mermaid
sequenceDiagram
    autonumber
    actor ユーザー
    participant UI as 画面 (HTML/DOM)
    participant Mgr as SubscriptionManager
    participant State as appState (ガチャ・シール状態)
    participant Storage as localStorage

    %% 1. 解約によるメダル獲得フロー
    ユーザー->>UI: サブスクの「解約」ボタンをクリック
    UI->>Mgr: cancel(id)
    Note over Mgr: 累計支出を計算<br>(1000円ごとに1枚 / 最低保証3枚)
    Mgr->>State: 算出されたメダル数を加算
    Mgr->>Storage: サブスクデータを保存
    State->>Storage: 最新のメダル枚数を保存 (saveState)
    UI->>ユーザー: 月額コストの減少とメダル増加を画面に反映

    %% 2. ガチャ＆デコレーションフロー
    ユーザー->>UI: 「ガチャを回す」ボタンをクリック
    UI->>State: playGacha()
    Note over State: ownedMedalsを1減算し<br>gachaPoolからランダム抽選
    State->>Storage: インベントリ(inventory)を保存 (saveState)
    UI->>ユーザー: ガチャ当選結果（絵文字）を表示
    ユーザー->>UI: リストのシールを選択し、画面の好きな場所をクリック
    UI->>State: handleGlobalClick(event)
    Note over State: 対象のinventoryを1減算し<br>クリックされた絶対座標(x,y)を保存
    State->>Storage: 配置データを保存 (saveState)
    UI->>ユーザー: 画面の指定した座標に絵文字シールを生成

    ### 4. 状態遷移図 (State Diagram)
ユーザーがシールを「選択していない状態」から、「貼るモード」「配置完了」「ダブルクリックでの回収」にいたるまでの、アプリケーションのデコレーション状態の変化を表した図です。

```markdown
### 🔄 状態遷移図

```mermaid
stateDiagram-v2
    [*] --> 未選択状態 : アプリ起動 (初期状態)

    未選択状態 --> 貼るモード中 : 所持枚数が1枚以上の\nシールをリストからクリック
    
    state 貼るモード中 {
        [*] --> 配置待機状態
        配置待機状態 --> シール配置完了 : 画面の好きな場所を\nクリック (座標記録 & 枚数消費)
    }

    貼るモード中 --> 貼るモード中 : まだ所持枚数が残っていれば\n連続してペタペタ貼れる
    貼るモード中 --> 未選択状態 : 所有枚数が0になる、または\n再度同じシールをクリックして解除

    シール配置完了 --> 未選択状態 : 自動的にリセット

    %% シールの回収フロー
    未選択状態 --> シール回収 : 画面上のシールを\nダブルクリック
    シール回収 --> 未選択状態 : 内部配列から削除され、\nリストの枚数が+1されて復元



    ## 🧪 テスト実行結果一覧

すべての項目においてテストを実施し、正常に機能が動作することを確認しました。

| # | テスト対象 | テスト観点(正常/境界/異常) | テスト条件 | テスト手順(1行) | 期待値(1行) | 結果(○/×) |
|---|---|---|---|---|---|---|
| 1 | サブスク登録 | 正常系 | 必須項目がすべて正しく入力されている状態 | 「Netflix」「1490」「月額」「2026-07-01」を入力して「登録する」を押す | 月額合計コストに「¥1,490」と反映され、一覧にカードが追加される | ○ |
| 2 | サブスク登録 | 正常系 | 年額サイクルのサブスクリプションを登録する状態 | 「プレミアムプラン」「12000」「年額」「2026-07-01」を入力して「登録する」を押す | 月額換算（12000/12）である「¥1,000」が月額合計コストに加算される | ○ |
| 3 | サブスク登録 | 境界入力 | アプリ名が1文字のみの状態 | アプリ名に「A」、金額「500」、日付を選択して「登録する」を押す | 正常に登録され、一覧に「A」という名前のカードが表示される | ○ |
| 4 | サブスク登録 | 境界入力 | 金額に「0」を入力した状態 | 金額に「0」、アプリ名「無料体験」、日付を選択して「登録する」を押す | 正常に登録され、合計コストは変動せず、価格「¥0」と表示される | ○ |
| 5 | サブスク登録 | 異常系 | アプリ名が空欄の状態 | アプリ名を空欄のまま、金額「1000」、日付を選択して「登録する」を押す | 「項目を入力してください」とアラートが表示され、登録処理が中断される | ○ |
| 6 | サブスク登録 | 異常系 | 金額が空欄の状態 | アプリ名「Spoti」、金額を空欄、日付を選択して「登録する」を押す | 「項目を入力してください」とアラートが表示され、登録処理が中断される | ○ |
| 7 | サブスク登録 | 異常系 | 開始日が未選択（空欄）の状態 | アプリ名「App」、金額「500」、日付を空欄にして「登録する」を押す | 「項目を入力してください」とアラートが表示され、登録処理が中断される | ○ |
| 8 | サブスク登録 | 異常系 | 金額にマイナスの数値を入力した状態 | 金額に「-500」、アプリ名「テスト」、日付を選択して「登録する」を押す | アラートが表示されて登録が拒否されるか、合計コストが不正に減算されない | ○ |
| 9 | サブスク一覧表示 | 正常系 | 特定の有名サービス（アイコン識別対象）を登録した状態 | アプリ名に「Netflix」と入力して登録し、生成されたカードのアイコンを確認する | アイコンの背景色が赤色（#e50914）になり、文字列が「Ne」と表示される | ○ |
| 10 | サブスク一覧表示 | 正常系 | 識別対象外のランダムなサービス名を登録した状態 | アプリ名に「XYZ」と入力して登録し、生成されたカードのアイコンを確認する | 文字列からハッシュ計算された固有の色が背景色として設定される | ○ |
| 11 | サブスク解約 | 正常系 | 登録中のサブスクリプションを解約する状態 | 一覧にあるカードの「解約」ボタンを押し、確認ダイアログで「OK」を押す | カードの不透明度下がり「(解約済)」となり、月額合計コストから引かれる | ○ |
| 12 | サブスク解約 | 正常系 | 解約に伴うメダル獲得の計算状態（累計支出1000円以上） | 2ヶ月以上前の開始日で金額「2500」のサブスクを登録し、即座に解約する | 累計支出に応じた枚数（2枚以上）が右上の手持ちメダル（ownedMedals）に加算される | ○ |
| 13 | サブスク解約 | 境界入力 | 解約に伴うメダル獲得の最低保証状態（累計支出が1000円未満） | 開始日を本日、金額「500」で登録したサブスクを即座に解約する | 1000円未満だが、最低保証ルールにより手持ちメダルが「3枚」加算される | ○ |
| 14 | サブスク削除 | 正常系 | 登録中（または解約済）のカードを完全に削除する状態 | 一覧にあるカードの「削除」ボタンを押し、確認ダイアログで「OK」を押す | 対象のサブスクカードが画面上および内部配列から完全に消滅する | ○ |
| 15 | タブ切り替え | 正常系 | 管理デスクからカレンダー表示へ切り替える状態 | 画面上部の「カレンダー表示」タブボタンをクリックする | 31日分のタイムラインが描画され、登録中サブスクの更新日（日付一致）にタグが表示される | ○ |
| 16 | アニマルガチャ | 正常系 | メダルを所持している状態でガチャを実行する状態 | 解約等でメダルを「1」以上にした状態で「ガチャを回す」ボタンを押す | メダルが1枚減り、中央ウィンドウに動物絵文字と獲得メッセージが表示される | ○ |
| 17 | アニマルガチャ | 異常系 | メダルが「0」の状態でガチャを実行する状態 | 手持ちメダルが0の状態で「ガチャを回す」ボタンを押す | メダル不足のアラートが表示され、減算および抽選処理は行われない | ○ |
| 18 | 所持シールリスト | 正常系 | ガチャで新しいシール（未獲得）を初めて引いた状態 | 未獲得のシールが出るまでガチャを回し、下のスクロールボックスを確認する | 対象シールの「❓/未獲得」表示が解除され、絵文字・名称・「1枚」バッジが表示される | ○ |
| 19 | 所持シールリスト | 境界入力 | すでに所持しているシールが重複（ダブり）して当選した状態 | 同一の動物が2回当たるまでガチャを回し、そのシールのバッジを確認する | シール自体の枠は増えず、対象カードの右上バッジの枚数が「2枚」にインクリメントされる | ○ |
| 20 | シール貼るモード | 正常系 | 所持しているシールを選択して配置待機状態にする状態 | 所持枚数が1枚以上のシールをリスト内から1回クリックする | 対象シールの枠がピンク色に強調され、上部に「選択中: [絵文字] (貼るモード)」と表示される | ○ |
| 21 | シール貼るモード | 正常系 | 貼るモード中に再度同じシールをクリックして解除する状態 | 選択状態（ピンク枠）になっているシールをもう一度リスト内からクリックする | 選択が解除されて枠が元に戻り、上部の表示が「選択中: なし」に切り替わる | ○ |
| 22 | シール配置（デコ） | 正常系 | 画面上の任意の空いている場所をクリックして配置する状態 | シールを選択した状態で、アプリ左側の管理デスクカード上の任意の点をクリックする | クリックした絶対座標を中心に選択した動物の絵文字が配置され、所持数が1枚減る | ○ |
| 23 | シール配置（デコ） | 境界入力 | 所持枚数が最後の1枚のシールを画面に配置した状態 | 所持数が「1枚」のシールを選択し、画面上の任意の場所をクリックする | 画面にシールが配置され、所持数が0になるため自動的に選択中（貼るモード）が解除される | ○ |
| 24 | シール回収 | 正常系 | 画面に貼り付けたシールを剥がしてリストに戻す状態 | 画面上に配置されている動物の絵文字を正確に「ダブルクリック」する | 画面上の該当シールが消滅し、右側リストの対象シールの所持枚数が「1枚」増える | ○ |

## 🛠️ 不合格ケースの対応レポート
開発段階の初期テストで一時的に不合格（×）となったケースについて、以下の通り修正・対応を完了し、再テストで全て合格（○）となることを確認しました。

### 【ケース8・ケース21・ケース24について】
* **原因:** 入力欄のマイナス値バリデーション漏れ、および貼るモードのトグル解除時・ダブルクリック回収時のイベント競合。
* **対応:** JavaScriptロジックを全面的に見直し、不適切な入力のブロックとイベント発火順序の適正化を最新コード（index.html）へ反映済み。